'use client';
import * as React from 'react'; import { Button, Card, CardContent, CardHeader, CardTitle, Input, Label } from '@cardioline/ui'; import { PrototypeToast } from '@/components/ui/prototype-toast';
export default function SecurityPage(){const [toast,setToast]=React.useState<string|null>(null);return <Card className="border-gray-200 bg-white"><CardHeader><CardTitle>Security</CardTitle></CardHeader><CardContent className="space-y-5"><p className="text-sm text-gray-500">Update the mock account password and sign-in preferences.</p>{['Current password','New password','Confirm password'].map((label)=><div key={label} className="space-y-2"><Label htmlFor={label}>{label}</Label><Input id={label} type="password" /></div>)}<div className="flex justify-end"><Button onClick={()=>setToast('Password updated.')} className="bg-primary text-white">Update password</Button></div><PrototypeToast message={toast} onClose={()=>setToast(null)} /></CardContent></Card>}
