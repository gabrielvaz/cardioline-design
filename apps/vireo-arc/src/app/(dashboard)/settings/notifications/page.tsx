'use client';
import * as React from 'react'; import { Button, Card, CardContent, CardHeader, CardTitle } from '@cardioline/ui'; import { PrototypeToast } from '@/components/ui/prototype-toast';
const items=['Critical ECG findings','Report ready for review','Device status changes']; export default function NotificationsPage(){const [toast,setToast]=React.useState<string|null>(null);return <Card className="border-gray-200 bg-white"><CardHeader><CardTitle>Notification preferences</CardTitle></CardHeader><CardContent>{items.map((item,index)=><label key={item} className="flex items-center justify-between border-t border-gray-100 py-4 text-sm font-medium"><span>{item}</span><input defaultChecked={index<2} type="checkbox" className="h-4 w-4 accent-[#ee5b00]" /></label>)}<div className="mt-4 flex justify-end"><Button onClick={()=>setToast('Notification preferences saved.')} className="bg-[#ee5b00] text-white">Save changes</Button></div><PrototypeToast message={toast} onClose={()=>setToast(null)} /></CardContent></Card>}
