'use client';
import * as React from 'react'; import { Camera } from 'lucide-react'; import { Button, Card, CardContent, CardHeader, CardTitle, Input, Label } from '@cardioline/ui'; import { PrototypeToast } from '@/components/ui/prototype-toast'; import { ThemeModeSelector } from '@/components/theme/theme-mode-selector';
export default function ProfilePage(){const [toast,setToast]=React.useState<string|null>(null);return <Card className="border-gray-200 bg-white shadow-sm"><CardHeader className="border-b border-gray-100"><CardTitle>Public profile</CardTitle></CardHeader><CardContent className="space-y-6 pt-6"><div className="flex items-center gap-5"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-[#071046] text-xl font-semibold text-white">SJ</div><Button variant="outline" onClick={()=>setToast('Avatar updated for this prototype.')}><Camera className="mr-2" />Change avatar</Button></div><div className="grid gap-5 md:grid-cols-2"><Field label="First name" value="Sarah"/><Field label="Last name" value="Jenkins"/><Field label="Email address" value="s.jenkins@hospital.com" wide/><Field label="Professional title" value="Senior Cardiologist" wide/></div><section className="flex flex-col gap-4 rounded-lg border border-gray-200 bg-gray-50/70 p-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-base font-semibold text-[#071046]">Appearance</h2><p className="mt-1 text-sm text-gray-500">Choose the color mode used throughout the platform.</p></div><ThemeModeSelector /></section><div className="flex justify-end border-t border-gray-100 pt-5"><Button onClick={()=>setToast('Profile changes saved.')} className="bg-primary text-white">Save changes</Button></div></CardContent><PrototypeToast message={toast} onClose={()=>setToast(null)} /></Card>};
function Field({label,value,wide=false}:{label:string;value:string;wide?:boolean}){const id=label.replaceAll(' ','-');return <div className={`space-y-2 ${wide?'md:col-span-2':''}`}><Label htmlFor={id}>{label}</Label><Input id={id} defaultValue={value}/></div>}
