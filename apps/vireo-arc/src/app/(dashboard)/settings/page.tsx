'use client';
import * as React from 'react'; import { Camera } from 'lucide-react'; import { Button, Card, CardContent, CardHeader, CardTitle, Input, Label } from '@cardioline/ui'; import { PrototypeToast } from '@/components/ui/prototype-toast';
export default function ProfilePage(){const [toast,setToast]=React.useState<string|null>(null);return <Card className="border-gray-200 bg-white shadow-sm"><CardHeader className="border-b border-gray-100"><CardTitle>Public profile</CardTitle></CardHeader><CardContent className="space-y-6 pt-6"><div className="flex items-center gap-5"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-[#071046] text-xl font-semibold text-white">SJ</div><Button variant="outline" onClick={()=>setToast('Avatar updated for this prototype.')}><Camera className="mr-2" />Change avatar</Button></div><div className="grid gap-5 md:grid-cols-2"><Field label="First name" value="Sarah"/><Field label="Last name" value="Jenkins"/><Field label="Email address" value="s.jenkins@hospital.com" wide/><Field label="Professional title" value="Senior Cardiologist" wide/></div><div className="flex justify-end border-t border-gray-100 pt-5"><Button onClick={()=>setToast('Profile changes saved.')} className="bg-[#ee5b00] text-white">Save changes</Button></div></CardContent><PrototypeToast message={toast} onClose={()=>setToast(null)} /></Card>};
function Field({label,value,wide=false}:{label:string;value:string;wide?:boolean}){const id=label.replaceAll(' ','-');return <div className={`space-y-2 ${wide?'md:col-span-2':''}`}><Label htmlFor={id}>{label}</Label><Input id={id} defaultValue={value}/></div>}
