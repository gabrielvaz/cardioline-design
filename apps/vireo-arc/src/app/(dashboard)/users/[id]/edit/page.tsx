'use client';
import { useRouter } from 'next/navigation'; import { ArrowLeft } from 'lucide-react'; import { Button, Card, CardContent, CardHeader, CardTitle, Input, Label } from '@cardioline/ui';
export default function EditUserPage() { const router = useRouter(); return <div className="mx-auto max-w-3xl space-y-6"><Button variant="ghost" onClick={() => router.back()}><ArrowLeft className="mr-2" />Back to users</Button><Card className="border-gray-200 bg-white"><CardHeader><CardTitle>Edit user</CardTitle></CardHeader><CardContent className="grid gap-5 md:grid-cols-2"><Field id="name" label="Name" value="Carlos Almeida" /><Field id="role" label="Role" value="Clinician" /><Field id="email" label="Email" value="c.almeida@hospital.com" /><Field id="status" label="Status" value="Pending" /><div className="col-span-full flex justify-end gap-3"><Button variant="outline" onClick={() => router.back()}>Cancel</Button><Button onClick={() => router.push('/users')} className="bg-primary text-white">Save user</Button></div></CardContent></Card></div>; }
function Field({ id, label, value }: { id:string; label:string; value:string }) { return <div className="space-y-2"><Label htmlFor={id}>{label}</Label><Input id={id} defaultValue={value} /></div>; }
