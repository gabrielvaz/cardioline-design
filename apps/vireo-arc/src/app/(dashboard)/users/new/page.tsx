'use client';
import { useRouter } from 'next/navigation'; import { ArrowLeft } from 'lucide-react'; import { Button, Card, CardContent, CardHeader, CardTitle, Input, Label } from '@cardioline/ui';
const fields = [['username', 'Username'], ['firstName', 'First name'], ['lastName', 'Last name'], ['birthday', 'Birthday'], ['title', 'Title'], ['specialization', 'Specialization'], ['registration', 'Professional registration'], ['email', 'Email'], ['phone', 'Phone'], ['membership', 'Membership'], ['code', 'Code']];
export default function NewUserPage() { const router = useRouter(); return <div className="space-y-6"><div><p className="text-sm text-gray-500">Administration › Users › Add user</p><h1 className="mt-1 text-2xl font-bold text-[#071046]">Create User</h1></div><Card className="border-gray-200 bg-white shadow-sm"><CardHeader><CardTitle className="text-base">Personal information</CardTitle></CardHeader><CardContent><div className="grid gap-5 md:grid-cols-3">{fields.slice(0, 5).map(([id, label]) => <Field key={id} id={id} label={label} type={id === 'birthday' ? 'date' : 'text'} />)}<div className="space-y-2"><Label htmlFor="auth">Authentication type</Label><select id="auth" className="h-10 w-full rounded-md border border-gray-200 bg-white px-3 text-sm"><option>Select an option</option><option>Password</option><option>Single sign-on</option></select></div><Field id="password" label="Password" type="password" /><Field id="confirmPassword" label="Confirm password" type="password" /></div></CardContent></Card><Card className="border-gray-200 bg-white shadow-sm"><CardHeader><CardTitle className="text-base">Additional information</CardTitle></CardHeader><CardContent><div className="grid gap-5 md:grid-cols-3">{fields.slice(5).map(([id, label]) => <Field key={id} id={id} label={label} type={id === 'email' ? 'email' : 'text'} />)}<div className="space-y-2 md:col-span-2"><Label htmlFor="note">Note</Label><textarea id="note" className="min-h-24 w-full rounded-md border border-gray-200 p-3 text-sm" placeholder="Type here" /></div></div><div className="mt-7 flex justify-end gap-3"><Button variant="outline" onClick={() => router.back()}>Cancel</Button><Button onClick={() => router.push('/users')} className="bg-primary text-white">Create user</Button></div></CardContent></Card></div>; }
function Field({ id, label, type }: { id: string; label: string; type: string }) { return <div className="space-y-2"><Label htmlFor={id}>{label}</Label><Input id={id} type={type} placeholder="Type here" /></div>; }
