import { ExamLoadingScreen } from "@/components/exams/exam-loading-gate";

/** Shown immediately while Next loads the selected exam route. */
export default function LoadingExam() {
  return <ExamLoadingScreen />;
}
